# 🚀 生产环境启动指南

> **更新时间**: 2025年11月5日  
> **版本**: v2.0  
> **适用场景**: 使用 cpolar 内网穿透的生产环境部署

---

## 📋 快速启动（三步走）

### ⚡ 第一步：检查 cpolar 地址

打开浏览器访问: **http://localhost:9200**

记录下两个关键地址：
- **后端隧道** (website): `https://xxxxxxxx.r30.cpolar.top` → `localhost:8000`
- **前端隧道** (frontend): `https://xxxxxxxx.r30.cpolar.top` → `localhost:4174`

⚠️ **重要**: cpolar 免费版地址每次重启都会变化！

---

### ⚡ 第二步：更新配置文件

#### 📝 后端配置 (`financial-analysis-api/.env`)

找到并更新以下两行（使用你的实际 cpolar 地址）：

```properties
# 必须包含前端和后端两个 cpolar 地址
ALLOW_ORIGINS=http://localhost:5173,http://127.0.0.1:5173,http://localhost:4174,http://127.0.0.1:4174,https://后端cpolar地址.r30.cpolar.top,https://前端cpolar地址.r30.cpolar.top

# 后端的公网地址
PUBLIC_API_BASE_URL=https://后端cpolar地址.r30.cpolar.top
```

**示例**（假设后端地址是 `4509929a`，前端地址是 `4b3f4740`）：
```properties
ALLOW_ORIGINS=http://localhost:5173,http://127.0.0.1:5173,http://localhost:4174,http://127.0.0.1:4174,https://4509929a.r30.cpolar.top,https://4b3f4740.r30.cpolar.top
PUBLIC_API_BASE_URL=https://4509929a.r30.cpolar.top
```

#### 📝 前端配置 (`financial-analysis-admin/.env.pro`)

找到并更新（注意必须包含 `/api` 后缀）：

```bash
# 后端 API 地址（必须加 /api 后缀！）
VITE_API_BASE_URL=https://后端cpolar地址.r30.cpolar.top/api
```

**示例**：
```bash
VITE_API_BASE_URL=https://4509929a.r30.cpolar.top/api
```

---

### ⚡ 第三步：启动服务

#### 🔴 启动后端（Terminal 1）

```powershell
# 进入后端目录
cd d:\比赛\金融分析项目\financial_analysis\financial-analysis-api

# 激活 conda 环境
conda activate financial-analysis

# 启动后端服务
python main.py
```

**✅ 成功标志**：
```
INFO:     Started server process
INFO:     Waiting for application startup.
INFO:     Application startup complete.
INFO:     Uvicorn running on http://0.0.0.0:8000 (Press CTRL+C to quit)
```

**🔍 验证后端**：
- 本地: http://localhost:8000/docs
- 公网: https://你的后端cpolar地址.r30.cpolar.top/docs

---

#### 🔵 启动前端（Terminal 2）

```powershell
# 进入前端目录
cd d:\比赛\金融分析项目\financial_analysis\financial-analysis-admin

# 清理旧构建（可选但推荐）
Remove-Item -Recurse -Force dist-pro -ErrorAction SilentlyContinue

# 重新构建生产版本（使用 .env.pro 配置）
pnpm run build:prod

# 启动预览服务器
pnpm vite preview --port 4174 --host 0.0.0.0 --outDir dist-pro
```

**✅ 成功标志**：
```
➜  Local:   http://localhost:4174/
➜  Network: http://192.168.x.x:4174/
```

**🔍 访问地址**：
- 本地: http://localhost:4174
- 公网: https://你的前端cpolar地址.r30.cpolar.top

---

## 🎯 完整启动命令速查

### 开发环境（本地调试）

```powershell
# Terminal 1: 后端
cd d:\比赛\金融分析项目\financial_analysis\financial-analysis-api
conda activate financial-analysis
python main.py

# Terminal 2: 前端（开发模式）
cd d:\比赛\金融分析项目\financial_analysis\financial-analysis-admin
pnpm run dev
```

**访问**: http://localhost:5173

---

### 生产环境（cpolar 公网访问）

```powershell
# Terminal 1: 后端
cd d:\比赛\金融分析项目\financial_analysis\financial-analysis-api
conda activate financial-analysis
python main.py

# Terminal 2: 前端（生产构建+预览）
cd d:\比赛\金融分析项目\financial_analysis\financial-analysis-admin
pnpm run build:prod
pnpm vite preview --port 4174 --host 0.0.0.0 --outDir dist-pro
```

**访问**: https://前端cpolar地址.r30.cpolar.top

---

## 🔧 当 cpolar 地址变化时

cpolar 免费版每次重启地址都会变化，需要执行以下步骤：

### 1️⃣ 查看新地址
访问 http://localhost:9200 查看新的 cpolar 地址

### 2️⃣ 更新配置文件

**后端** `financial-analysis-api/.env`:
```properties
ALLOW_ORIGINS=...,https://新后端地址.r30.cpolar.top,https://新前端地址.r30.cpolar.top
PUBLIC_API_BASE_URL=https://新后端地址.r30.cpolar.top
```

**前端** `financial-analysis-admin/.env.pro`:
```bash
VITE_API_BASE_URL=https://新后端地址.r30.cpolar.top/api
```

### 3️⃣ 重启服务

```powershell
# 1. 停止旧的后端和前端进程（Ctrl+C）

# 2. 重启后端
cd d:\比赛\金融分析项目\financial_analysis\financial-analysis-api
conda activate financial-analysis
python main.py

# 3. 重新构建并启动前端
cd d:\比赛\金融分析项目\financial_analysis\financial-analysis-admin
pnpm run build:prod
pnpm vite preview --port 4174 --host 0.0.0.0 --outDir dist-pro
```

---

## 🐛 常见问题排查

### ❌ 问题 1: CORS 错误

**错误信息**:
```
Access to XMLHttpRequest has been blocked by CORS policy: 
No 'Access-Control-Allow-Origin' header is present
```

**原因**: 
- 后端 CORS 配置中没有包含前端的 cpolar 地址
- 后端没有重启（修改 .env 后必须重启）

**解决方案**:
1. 检查 `financial-analysis-api/.env` 中的 `ALLOW_ORIGINS` 是否包含前端 cpolar 地址
2. **重启后端服务**（必须！）
3. 验证后端文档是否可以访问: https://后端cpolar地址.r30.cpolar.top/docs

---

### ❌ 问题 2: 前端请求到旧地址

**错误信息**:
```
GET https://旧地址.r30.cpolar.top/api/xxx 404
```

**原因**: 
- 更新了 `.env.pro` 但没有重新构建
- 使用了缓存的构建产物

**解决方案**:
```powershell
# 清理并重新构建
cd d:\比赛\金融分析项目\financial_analysis\financial-analysis-admin
Remove-Item -Recurse -Force dist-pro
pnpm run build:prod
pnpm vite preview --port 4174 --host 0.0.0.0 --outDir dist-pro
```

---

### ❌ 问题 3: 构建输出目录不匹配

**错误信息**:
```
The directory "dist-pro" does not exist. Did you build your project?
```

**原因**: 
- `pnpm run build:prod` 使用 `.env.pro` 配置，输出到 `dist-pro`
- 但 `vite.config.ts` 没有正确读取环境变量

**解决方案**:
已在 `vite.config.ts` 中添加了环境变量支持，确保 `.env.pro` 中有：
```bash
VITE_OUT_DIR=dist-pro
```

---

### ❌ 问题 4: 端口被占用

**错误信息**:
```
Port 8000 is already in use
Port 4174 is already in use
```

**解决方案**:
```powershell
# 查找并停止占用端口的进程
# 后端端口 8000
Get-NetTCPConnection -LocalPort 8000 -ErrorAction SilentlyContinue | 
  Select-Object -ExpandProperty OwningProcess -Unique | 
  ForEach-Object { Stop-Process -Id $_ -Force }

# 前端端口 4174
Get-NetTCPConnection -LocalPort 4174 -ErrorAction SilentlyContinue | 
  Select-Object -ExpandProperty OwningProcess -Unique | 
  ForEach-Object { Stop-Process -Id $_ -Force }
```

---

## 📊 环境变量配置速查表

### 后端 (`financial-analysis-api/.env`)

```properties
# 数据库配置
DATABASE_HOST=localhost
DATABASE_USER=root
DATABASE_PASSWORD=你的密码
DATABASE_NAME=financial_analysis_db
DATABASE_PORT=3306

# JWT 配置
SECRET_KEY=你的密钥
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# CORS 配置（关键！）
ALLOW_ORIGINS=http://localhost:5173,http://127.0.0.1:5173,http://localhost:4174,http://127.0.0.1:4174,https://后端cpolar地址.r30.cpolar.top,https://前端cpolar地址.r30.cpolar.top
PUBLIC_API_BASE_URL=https://后端cpolar地址.r30.cpolar.top

# 自动更新配置（可选）
ENABLE_AUTO_UPDATE=1
AUTO_UPDATE_TIME=20:30
AUTO_UPDATE_ON_STARTUP=1

# 默认管理员（可选）
ENABLE_DEFAULT_ADMIN=1

# Tushare Token（可选）
TUSHARE_TOKEN=你的token
```

### 前端 (`financial-analysis-admin/.env.pro`)

```bash
# 环境
VITE_NODE_ENV=production

# 后端 API 地址（必须包含 /api 后缀）
VITE_API_BASE_URL=https://后端cpolar地址.r30.cpolar.top/api

# 输出目录
VITE_OUT_DIR=dist-pro

# 应用标题
VITE_APP_TITLE=金融分析系统

# 构建优化
VITE_DROP_DEBUGGER=true
VITE_DROP_CONSOLE=true
VITE_SOURCEMAP=false
```

---

## 🎯 关键注意事项

1. ✅ **CORS 配置必须包含前端地址** - 后端 `ALLOW_ORIGINS` 要同时包含前端和后端的 cpolar 地址
2. ✅ **API 地址必须加 /api 后缀** - 前端配置 `VITE_API_BASE_URL` 必须以 `/api` 结尾
3. ✅ **修改配置后必须重启** - 后端修改 `.env` 必须重启；前端修改 `.env.pro` 必须重新构建
4. ✅ **cpolar 地址会变化** - 免费版每次重启都会变，需要及时更新配置
5. ✅ **环境变量在构建时注入** - 前端的环境变量在 `build` 时注入到代码中，不重新构建不会生效

---

## 🆘 需要帮助？

### 检查清单

- [ ] cpolar 是否正在运行？访问 http://localhost:9200
- [ ] 后端是否启动成功？访问 http://localhost:8000/docs
- [ ] 前端是否构建成功？检查是否有 `dist-pro` 目录
- [ ] 配置文件中的 cpolar 地址是否正确？
- [ ] 后端 CORS 是否包含前端地址？
- [ ] 前端 API 地址是否包含 `/api` 后缀？

### 日志查看

- **后端日志**: 直接在后端终端查看
- **前端日志**: 浏览器 F12 → Console / Network
- **cpolar 日志**: http://localhost:9200 → 日志页面

### 测试命令

```powershell
# 测试后端连接
curl http://localhost:8000/docs

# 测试 cpolar 后端连接
curl https://后端cpolar地址.r30.cpolar.top/docs

# 检查端口占用
netstat -ano | findstr :8000
netstat -ano | findstr :4174
```

---

## 📝 快速命令卡片

打印或保存以下命令备用：

```powershell
# ========== 启动服务 ==========
# 后端
cd d:\比赛\金融分析项目\financial_analysis\financial-analysis-api
conda activate financial-analysis
python main.py

# 前端
cd d:\比赛\金融分析项目\financial_analysis\financial-analysis-admin
pnpm run build:prod
pnpm vite preview --port 4174 --host 0.0.0.0 --outDir dist-pro

# ========== 更新 cpolar 地址后 ==========
# 1. 查看新地址: http://localhost:9200
# 2. 更新 financial-analysis-api/.env (ALLOW_ORIGINS, PUBLIC_API_BASE_URL)
# 3. 更新 financial-analysis-admin/.env.pro (VITE_API_BASE_URL)
# 4. 重启后端（Ctrl+C 后重新运行 python main.py）
# 5. 重新构建前端（pnpm run build:prod）
# 6. 重启前端预览（pnpm vite preview...）

# ========== 清理端口 ==========
Get-NetTCPConnection -LocalPort 8000 | Select -Expand OwningProcess | Stop-Process -Force
Get-NetTCPConnection -LocalPort 4174 | Select -Expand OwningProcess | Stop-Process -Force
```

---

<div align="center">

**🎉 祝部署顺利！**

如有问题请查看 [项目完成情况报告.md](./项目完成情况报告.md) 或 [README.md](./README.md)

</div>
