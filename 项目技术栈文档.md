# 📊 金融分析项目 - 完整技术栈文档

---

## 项目概述

这是一个基于 **Vue 3 + FastAPI** 的全栈金融数据分析系统，集成了用户管理、市场数据查询、技术分析、选股策略等完整功能。

---

## 🎨 前端技术栈

### 核心框架
- **Vue 3.3.4** - 渐进式 JavaScript 框架
- **TypeScript 5.0.2** - 类型安全的 JavaScript 超集
- **Vite 4.3.9** - 新一代前端构建工具

### UI 组件库
- **Element Plus 2.3.6** - 基于 Vue 3 的组件库
- **@element-plus/icons-vue 2.3.2** - Element Plus 图标库

### 状态管理与路由
- **Pinia 2.1.3** - Vue 3 官方状态管理库
- **Vue Router 4.2.2** - Vue 官方路由管理器

### 数据可视化
- **ECharts 6.0.0** - 强大的数据可视化图表库
  - K线图表
  - 技术指标图表
  - 交易量图表

### 网络请求
- **Axios 1.4.0** - HTTP 请求库

### 工具库
- **dayjs 1.11.18** - 轻量级日期处理库

### 开发工具
- **unplugin-auto-import 0.16.4** - 自动导入 API
- **unplugin-vue-components 0.25.1** - 自动导入组件
- **ESLint 8.43.0** - 代码规范检查
- **Prettier 2.8.8** - 代码格式化
- **Stylelint 15.10.0** - CSS 规范检查
- **Vue TSC 1.4.2** - Vue 类型检查

---

## 🚀 后端技术栈

### Web 框架
- **FastAPI 0.110.0** - 现代、高性能的 Python Web 框架
  - 自动生成 API 文档（Swagger UI / ReDoc）
  - 异步支持
  - 类型提示和数据验证
- **Uvicorn[standard] 0.27.1** - ASGI 服务器
- **Gunicorn 21.2.0** - WSGI HTTP 服务器（生产环境）

### 数据库 ORM
- **SQLAlchemy 2.0.27** - Python SQL 工具包和 ORM
  - 声明式模型定义
  - 原生 SQL 支持
  - 连接池管理
- **PyMySQL 1.1.0** - MySQL 数据库驱动

### 数据验证
- **Pydantic 2.6.1** - 数据验证和设置管理
  - 请求体验证
  - 响应模型定义
  - 配置管理

### 认证与加密
- **PyJWT 2.8.0** - JSON Web Token 实现
- **passlib 1.7.4** - 密码哈希库
- **bcrypt 3.2.0** - 密码加密算法

### 数据处理
- **pandas 2.2.3** - 数据分析和处理
- **numpy 2.2.2** - 科学计算库
- **openpyxl 3.1.2** - Excel 文件读写
- **XlsxWriter 3.1.9** - Excel 文件写入

### 金融数据源
- **tushare 1.3.10** - 金融数据接口
  - 历史数据更新
  - 交易日历
- **akshare** - 金融数据爬虫库（动态导入）
  - 实时行情数据
  - K线历史数据
  - 市场概况数据

### 任务调度
- **schedule 1.1.0** - Python 任务调度库
  - 定时数据更新
  - 后台任务执行

### 其他工具
- **python-dotenv 1.0.1** - 环境变量管理
- **loguru 0.7.2** - 日志记录
- **aiofiles 23.2.1** - 异步文件操作
- **python-multipart 0.0.9** - 文件上传处理

---

## 🗂️ 项目功能模块

### 1. 用户管理模块

#### 后端 API 接口
| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/auth/register` | 用户注册 |
| POST | `/api/auth/login` | 用户登录（返回 JWT Token）|
| GET | `/api/users/me` | 获取当前用户信息 |
| PUT | `/api/users/me/avatar` | 更新用户头像（Base64）|
| PUT | `/api/users/me/profile` | 更新用户资料 |
| POST | `/api/users/me/password` | 修改密码 |

#### 前端页面
- `LoginView.vue` - 登录页面
- `Profile.vue` - 用户资料页
- `Permissions.vue` - 权限管理

#### 核心功能
- JWT Token 认证
- 密码 bcrypt 加密
- 头像 Base64 存储
- 权限角色管理（admin/user）

---

### 2. 管理员模块

#### 后端 API 接口
| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/vadmin/users/` | 获取用户列表（分页、搜索）|
| POST | `/api/vadmin/users/` | 创建用户 |
| GET | `/api/vadmin/users/{user_id}` | 获取用户详情 |
| PUT | `/api/vadmin/users/{user_id}` | 更新用户信息 |
| PATCH | `/api/vadmin/users/{user_id}/password` | 管理员重置密码 |
| DELETE | `/api/vadmin/users/{user_id}` | 删除用户 |

#### 前端页面
- `admin/Users.vue` - 用户管理页面

#### 前端组件
- `UserEditDialog.vue` - 用户编辑对话框
- `UserViewDialog.vue` - 用户查看对话框

#### 核心功能
- 管理员权限验证
- 用户增删改查
- 批量操作
- 搜索过滤

---

### 3. 股票市场模块

#### 后端 API 接口
| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/stocks/trade_dates` | 获取交易日历 |
| GET | `/api/stocks/trade_dates_with_status` | 获取交易日历（含每日状态）|
| GET | `/api/stocks/realtime` | 获取单个股票实时行情 |
| POST | `/api/stocks/realtime/batch` | 批量获取实时行情 |
| GET | `/api/stocks/search` | 搜索股票（代码/名称）|
| GET | `/api/stocks/history` | 获取历史K线数据 |
| GET | `/api/stocks/sse_daily_summary` | 上交所每日概况 |
| GET | `/api/stocks/company_profile` | 获取公司资料 |
| GET | `/api/stocks/search_companies` | 按条件搜索公司（分页）|

#### 前端页面
- `market/Snapshot.vue` - 市场快照（公司概况查询）
- `market/MarketSummary.vue` - 市场概览
- `market/TechnicalAnalysis.vue` - 技术分析
- `stock/StockHistory.vue` - 个股历史数据

#### 核心功能
- 实时行情查询（AKShare）
- 历史数据查询（支持前复权/后复权）
- 公司基本信息（从数据库查询）
- 高级搜索筛选
  - 行业筛选
  - 地区筛选
  - 注册资本筛选
  - 模糊/精准搜索

---

### 4. 技术分析模块

#### 后端 API 接口
| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/stocks/technical_indicators` | 计算技术指标 |
| GET | `/api/stocks/indicator_config` | 获取指标配置 |
| GET | `/api/stocks/technical_signals` | 检测技术信号 |

#### 前端组件
- `TechnicalChart.vue` - 技术图表
- `TechnicalSignals.vue` - 技术信号
- `StockChart.vue` - 股票图表
- `OHLCVChart.vue` - OHLCV 图表

#### 支持的技术指标
- **MA** - 移动平均线
- **EMA** - 指数移动平均线
- **MACD** - 平滑异同移动平均线
- **RSI** - 相对强弱指数
- **KDJ** - 随机指标
- **BOLL** - 布林带
- **VOL** - 成交量

#### 核心功能
- 多种技术指标计算
- 买卖信号检测
- 图表可视化展示

---

### 5. 选股策略模块

#### 前端页面
- `picker/Filter.vue` - 条件筛选器
- `picker/Hot.vue` - 热门股票
- `picker/Backtest.vue` - 策略回测
- `strategy/IndustryRotation.vue` - 行业轮动

#### 核心功能
- 多条件组合筛选
- 策略回测分析
- 行业轮动监控

---

### 6. 自选股模块

#### 前端页面
- `watchlist/Groups.vue` - 自选分组
- `watchlist/Crud.vue` - 自选管理
- `watchlist/Alerts.vue` - 价格提醒

#### 核心功能
- 自选股分组管理
- 价格提醒设置
- 实时监控

---

### 7. 财务分析模块

#### 前端页面
- `finance/Reports.vue` - 财务报表
- `finance/Metrics.vue` - 财务指标
- `finance/Peer.vue` - 同业对比

#### 前端组件
- `FinancialMetrics.vue` - 财务指标展示

#### 核心功能
- 财务报表查看
- 财务指标分析
- 同业公司对比

---

### 8. 新闻资讯模块

#### 前端页面
- `news/Realtime.vue` - 实时资讯
- `news/Announcements.vue` - 公告信息

#### 核心功能
- 实时新闻推送
- 公司公告查看

---

### 9. 公司信息模块

#### 前端组件
- `company/CompanyHeader.vue` - 公司头部信息
- `company/CompanyInfoTable.vue` - 公司信息表格
- `company/CompanyKLine.vue` - 公司K线图
- `company/Shareholdings.vue` - 股东信息
- `company/ManagementTeam.vue` - 管理团队
- `company/IssueInfo.vue` - 发行信息

#### 核心功能
- 公司基本信息展示
- 股权结构分析
- 管理层信息查看

---

## 🔄 数据流架构

```
┌─────────────────────────────────────────────────────────────┐
│                    前端 (Vue 3 + Vite)                      │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐   │
│  │ 用户管理 │  │ 市场数据 │  │ 技术分析 │  │ 选股策略 │   │
│  └──────────┘  └──────────┘  └──────────┘  └──────────┘   │
└────────────────────────┬────────────────────────────────────┘
                         │ Axios HTTP 请求
                         ↓
┌─────────────────────────────────────────────────────────────┐
│              FastAPI 路由层 (app/apis/*.py)                 │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐   │
│  │  auth.py │  │ stock.py │  │  user.py │  │vadmin.py │   │
│  └──────────┘  └──────────┘  └──────────┘  └──────────┘   │
└────────────────────────┬────────────────────────────────────┘
                         │ 调用服务层
                         ↓
┌─────────────────────────────────────────────────────────────┐
│            Service 层 (app/services/*.py)                   │
│  ┌────────────────┐  ┌────────────────────────────────┐    │
│  │ auth_service   │  │      stock_service             │    │
│  │ - 用户认证     │  │  - 实时行情                    │    │
│  │ - JWT Token    │  │  - 历史数据                    │    │
│  │ - 密码加密     │  │  - 技术指标计算                │    │
│  └────────────────┘  └────────────────────────────────┘    │
└──────────┬──────────────────────┬──────────────────────────┘
           │                      │
           ↓                      ↓
┌──────────────────────┐  ┌──────────────────────────────────┐
│  SQLAlchemy + MySQL  │  │  AKShare / Tushare (第三方数据源)│
│  ┌────────────────┐  │  │  ┌────────────────────────────┐ │
│  │ users 表       │  │  │  │ - 实时行情 API             │ │
│  │ a_stock_info表 │  │  │  │ - 历史K线数据              │ │
│  │ stock_history表│  │  │  │ - 交易日历                 │ │
│  └────────────────┘  │  │  │ - 市场概况                 │ │
│  - 用户数据          │  │  └────────────────────────────┘ │
│  - 公司基本信息      │  │  - 东方财富网                    │
│  - 历史数据快照      │  │  - 新浪财经                      │
└──────────────────────┘  └──────────────────────────────────┘
```

---

## 🛠️ 核心技术方法

### 前端核心方法

#### 1. Vue 3 组合式 API
```typescript
import { ref, reactive, computed, onMounted } from 'vue'

const count = ref(0)
const state = reactive({ name: 'Vue' })
const doubleCount = computed(() => count.value * 2)

onMounted(() => {
  console.log('Component mounted')
})
```

#### 2. 响应式状态管理（Pinia）
```typescript
import { defineStore } from 'pinia'

export const useAuthStore = defineStore('auth', {
  state: () => ({
    token: '',
    user: null
  }),
  actions: {
    async login(credentials) {
      // 登录逻辑
    }
  }
})
```

#### 3. 路由守卫（权限验证）
```typescript
router.beforeEach((to, from, next) => {
  const authStore = useAuthStore()
  if (to.meta.requiresAuth && !authStore.token) {
    next('/login')
  } else {
    next()
  }
})
```

#### 4. Axios 请求拦截器
```typescript
// 请求拦截：注入 Token
service.interceptors.request.use(config => {
  const token = getToken()
  if (token) {
    config.headers.Authorization = `Bearer ${token}`
  }
  return config
})

// 响应拦截：处理错误
service.interceptors.response.use(
  response => response.data,
  error => {
    if (error.response?.status === 401) {
      // 跳转登录
    }
    return Promise.reject(error)
  }
)
```

#### 5. ECharts 图表配置
```typescript
const option = {
  xAxis: { type: 'category', data: dates },
  yAxis: { type: 'value' },
  series: [{
    type: 'candlestick',
    data: klineData
  }]
}
chart.setOption(option)
```

---

### 后端核心方法

#### 1. FastAPI 依赖注入
```python
from fastapi import Depends
from sqlalchemy.orm import Session
from app.core.database import get_db

@router.get("/users")
def get_users(db: Session = Depends(get_db)):
    return db.query(User).all()
```

#### 2. JWT 认证
```python
import jwt
from datetime import datetime, timedelta

def create_access_token(data: dict):
    to_encode = data.copy()
    expire = datetime.utcnow() + timedelta(minutes=30)
    to_encode.update({"exp": expire})
    return jwt.encode(to_encode, SECRET_KEY, algorithm=ALGORITHM)

def get_current_user(token: str = Depends(oauth2_scheme)):
    payload = jwt.decode(token, SECRET_KEY, algorithms=[ALGORITHM])
    return payload
```

#### 3. 密码加密
```python
from passlib.context import CryptContext

pwd_context = CryptContext(schemes=["bcrypt"], deprecated="auto")

def hash_password(password: str) -> str:
    return pwd_context.hash(password)

def verify_password(plain: str, hashed: str) -> bool:
    return pwd_context.verify(plain, hashed)
```

#### 4. SQLAlchemy ORM 查询
```python
from sqlalchemy import text

# 原生 SQL
result = db.execute(
    text("SELECT * FROM a_stock_info WHERE stock_code = :code"),
    {"code": "000001"}
).mappings().fetchall()

# ORM 查询
user = db.query(User).filter(User.username == "admin").first()
```

#### 5. 数据清洗（Pandas）
```python
import pandas as pd
import numpy as np

def clean_dataframe(df: pd.DataFrame) -> pd.DataFrame:
    # 处理 NaN 和 inf
    df = df.replace([np.nan, np.inf, -np.inf], None)
    
    # 格式化日期
    if 'date' in df.columns:
        df['date'] = pd.to_datetime(df['date']).dt.strftime('%Y-%m-%d')
    
    return df
```

#### 6. 异步任务调度
```python
import schedule
import threading

def job():
    print("执行定时任务")

# 每天 20:00 执行
schedule.every().day.at("20:00").do(job)

def run_scheduler():
    while True:
        schedule.run_pending()
        time.sleep(60)

# 后台线程运行
threading.Thread(target=run_scheduler, daemon=True).start()
```

#### 7. AKShare 数据获取
```python
import akshare as ak

# 获取实时行情
df = ak.stock_individual_info_em(symbol="000001")

# 获取历史K线
df = ak.stock_zh_a_hist(
    symbol="000001",
    start_date="20230101",
    end_date="20231231",
    adjust="qfq"  # 前复权
)
```

---

## 📁 项目目录结构

### 前端目录
```
financial-analysis-admin/
├── src/
│   ├── api/              # API 接口定义
│   │   ├── auth.ts
│   │   ├── stock.ts
│   │   └── user.ts
│   ├── components/       # 公共组件
│   │   ├── atomic/       # 原子组件
│   │   ├── business/     # 业务组件
│   │   ├── company/      # 公司信息组件
│   │   └── admin/        # 管理组件
│   ├── views/            # 页面视图
│   │   ├── admin/        # 管理页面
│   │   ├── finance/      # 财务分析
│   │   ├── market/       # 市场数据
│   │   ├── stock/        # 股票详情
│   │   └── user/         # 用户中心
│   ├── store/            # Pinia 状态管理
│   ├── router/           # 路由配置
│   ├── utils/            # 工具函数
│   │   ├── request.ts    # Axios 封装
│   │   ├── format.ts     # 格式化工具
│   │   └── date.ts       # 日期处理
│   ├── types/            # TypeScript 类型定义
│   └── config/           # 配置文件
└── vite.config.ts        # Vite 配置
```

### 后端目录
```
financial-analysis-api/
├── app/
│   ├── apis/             # API 路由
│   │   ├── auth.py       # 认证接口
│   │   ├── stock.py      # 股票接口
│   │   ├── user.py       # 用户接口
│   │   └── vadmin_users.py # 管理员接口
│   ├── services/         # 业务逻辑层
│   │   ├── auth_service.py
│   │   ├── stock_service.py
│   │   └── technical_indicators.py
│   ├── models/           # 数据库模型
│   │   └── user.py
│   ├── schemas/          # Pydantic 模型
│   │   └── auth.py
│   ├── core/             # 核心配置
│   │   ├── database.py   # 数据库连接
│   │   ├── dependencies.py
│   │   └── middleware.py
│   └── utils/            # 工具函数
├── scripts/              # 脚本
│   ├── auto_update.py    # 自动更新数据
│   └── initialize/       # 初始化脚本
├── logs/                 # 日志文件
├── main.py               # 应用入口
└── requirements.txt      # 依赖列表
```

---

## 🚀 部署说明

### 前端部署
```bash
# 安装依赖
pnpm install

# 开发环境
pnpm dev

# 生产构建
pnpm build

# 预览构建结果
pnpm preview
```

### 后端部署
```bash
# 安装依赖
pip install -r requirements.txt

# 开发环境
python main.py

# 生产环境（使用 Uvicorn）
uvicorn main:app --host 0.0.0.0 --port 8000

# 生产环境（使用 Gunicorn + Uvicorn Workers）
gunicorn main:app -w 4 -k uvicorn.workers.UvicornWorker --bind 0.0.0.0:8000
```

### 环境变量配置（.env）
```env
# 数据库配置
DATABASE_HOST=localhost
DATABASE_PORT=3306
DATABASE_USER=root
DATABASE_PASSWORD=your_password
DATABASE_NAME=financial_analysis_db

# JWT 配置
SECRET_KEY=your-secret-key-here
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# 自动更新配置（可选）
ENABLE_AUTO_UPDATE=0
AUTO_UPDATE_ON_STARTUP=0
AUTO_UPDATE_TIME=20:00

# 默认管理员账号（仅开发环境）
ENABLE_DEFAULT_ADMIN=0

# Tushare Token（可选）
TUSHARE_TOKEN=your_tushare_token
```

---

## 📊 数据库设计

### 主要数据表

#### users（用户表）
| 字段 | 类型 | 说明 |
|------|------|------|
| id | INT | 主键 |
| username | VARCHAR(50) | 用户名（唯一）|
| hashed_password | VARCHAR(255) | 密码哈希 |
| avatar | TEXT | 头像（Base64）|
| nickname | VARCHAR(100) | 昵称 |
| phone | VARCHAR(20) | 手机号 |
| email | VARCHAR(100) | 邮箱 |
| signature | VARCHAR(200) | 个性签名 |
| role | VARCHAR(20) | 角色（admin/user）|
| created_at | DATETIME | 创建时间 |

#### a_stock_info（A股信息表）
| 字段 | 类型 | 说明 |
|------|------|------|
| stock_code | VARCHAR(10) | 股票代码 |
| a_stock_abbr | VARCHAR(50) | 股票简称 |
| company_name | VARCHAR(200) | 公司名称 |
| security_category | VARCHAR(50) | 证券类型 |
| chairman | VARCHAR(50) | 董事长 |
| legal_representative | VARCHAR(50) | 法人代表 |
| region | VARCHAR(50) | 地区 |
| registered_capital | VARCHAR(50) | 注册资本 |
| eastmoney_industry | VARCHAR(100) | 东财行业 |
| regulatory_industry | VARCHAR(100) | 证监会行业 |
| listing_exchange | VARCHAR(20) | 上市交易所 |

---

## 🔒 安全措施

1. **密码安全**
   - bcrypt 加密存储
   - 密码强度验证

2. **Token 认证**
   - JWT Token 过期机制
   - Token 刷新策略

3. **CORS 配置**
   - 允许的源控制
   - 凭证传递

4. **SQL 注入防护**
   - 参数化查询
   - ORM 使用

5. **XSS 防护**
   - 输入验证
   - 输出转义

---

## 📈 性能优化

### 前端优化
1. **代码分割** - Vite 自动代码分割
2. **组件懒加载** - 路由级别懒加载
3. **依赖预构建** - Vite optimizeDeps
4. **图表性能** - ECharts 数据采样和渲染优化

### 后端优化
1. **数据缓存** - 历史数据内存缓存
2. **连接池** - SQLAlchemy 连接池管理
3. **异步处理** - FastAPI 异步支持
4. **数据库索引** - 关键字段索引优化

---

## 📝 更新日志

### v1.0.0 (2025-11-01)
- ✅ 完成用户认证系统
- ✅ 完成股票数据查询功能
- ✅ 完成技术分析模块
- ✅ 完成管理员后台
- ✅ 支持局域网访问
- ✅ 优化 Vite 开发服务器配置

---

## 👥 开发团队

- **项目负责人**: LiQiCheng457
- **GitHub**: https://github.com/LiQiCheng457/financial-analysis

---

## 📄 许可证

本项目仅供学习和研究使用。

---

**文档生成时间**: 2025年11月2日
